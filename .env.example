# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=github_events
DB_USER=postgres
DB_PASSWORD=postgres

# GitHub API
# Optional but recommended to avoid rate limits:
# Create a fine-grained token OR classic token with public access.
GITHUB_TOKEN=
GITHUB_USER_AGENT=github-events-pipeline

# Ingest settings
INGEST_LIMIT=50
